## 1. 目录结构重组

- [x] 1.1 创建 `server/res/` 目录
- [x] 1.2 移动静态资源 `server/web/` → `server/res/web/`
- [x] 1.3 更新 `server/.gitignore` 忽略 `res/web/*`

## 2. 静态资源嵌入

- [x] 2.1 创建 `server/res/res.go` 定义 embed 变量
- [x] 2.2 导出 `WebFS` 和 `WebAssetsFS` 文件系统
- [x] 2.3 更新 `main.go` 使用 `r.StaticFS()` 和 `http.ServeFileFS()`

## 3. Session 管理重构

- [x] 3.1 添加依赖 `gin-contrib/sessions` 和 `quasoft/memstore`
- [x] 3.2 更新 `main.go` 配置 session 中间件
- [x] 3.3 重构 `middleware/auth.go`：
  - [x] 3.3.1 移除 `InitSessionStore()` 函数
  - [x] 3.3.2 添加 `LoginUser()` / `LogoutUser()` / `GetUserID()` 辅助函数
- [x] 3.4 更新 `handler/auth.go` 使用新的辅助函数

## 4. CORS 中间件改进

- [x] 4.1 添加依赖 `gin-contrib/cors`
- [x] 4.2 替换手写 CORS 中间件为 `cors.Default()`

## 5. 模块名简化

- [x] 5.1 更新 `go.mod` 模块名为 `clash-server`
- [x] 5.2 批量更新所有 `.go` 文件的导入路径

## 6. 前端构建配置

- [x] 6.1 更新 `web/Makefile` 输出路径 `../server/res/web/`

## 7. 前端 UI 改进

### 7.1 PC 端登出入口

- [x] 7.1.1 在 `MainLayout.vue` 侧边栏底部添加用户信息区域
- [x] 7.1.2 显示用户名和退出登录按钮
- [x] 7.1.3 实现退出登录功能，跳转到登录页

### 7.2 仪表盘核心状态优化

- [x] 7.2.1 修改 `RuntimeInfoCard.vue` 核心状态显示逻辑
- [x] 7.2.2 运行时只显示版本号，停止时显示"已停止"

### 7.3 订阅卡片激活样式优化

- [x] 7.3.1 移除 `SubscriptionCard.vue` 激活状态的绿色边框
- [x] 7.3.2 改用左侧 3px 绿色竖线指示激活状态

### 7.4 规则编辑器删除按钮优化

- [x] 7.4.1 修改 `RuleEditorDialog.vue` 小屏幕布局
- [x] 7.4.2 将删除按钮移至规则项右上角（仅小屏幕）

### 7.5 侧边栏流量组件

- [x] 7.5.1 创建 `SidebarTraffic.vue` 组件
- [x] 7.5.2 实现迷你流量图表（贝塞尔曲线，右对齐，30秒历史）
- [x] 7.5.3 使用 Element Plus 图标（Upload/Download/Cpu）
- [x] 7.5.4 显示上行/下行/内存使用量
- [x] 7.5.5 每秒采集数据，与仪表盘图表一致

### 7.6 设置页面优化

- [x] 7.6.1 设置卡片宽度改为 100%

### 7.7 代理延迟颜色编码

- [x] 7.7.1 根据延迟值显示不同颜色（绿/黄绿/黄/橙/红/灰）

### 7.8 代理组样式优化

- [x] 7.8.1 代理组添加边框框架
- [x] 7.8.2 选中代理显示蓝色角标

### 7.9 连接信息显示优化

- [x] 7.9.1 两行布局显示连接详情
- [x] 7.9.2 使用边框框架包裹
- [x] 7.9.3 添加连接开始时间显示（格式 `HH:mm:ss.SSS`）
- [x] 7.9.4 时间显示在主机名前，用 ` - ` 分隔
- [x] 7.9.5 时间和主机名字体大小一致，颜色区分

### 7.10 连接数统计优化

- [x] 7.10.1 仅显示激活的连接数
- [x] 7.10.2 使用 Link 图标

### 7.11 日志格式优化

- [x] 7.11.1 时间和级别使用方框样式
- [x] 7.11.2 格式化显示日志内容
- [x] 7.11.3 统一时间格式为 `HH:mm:ss.SSS`，去掉日期
- [x] 7.11.4 大屏布局：`[时间] [level] payload` 同一行
- [x] 7.11.5 小屏布局：时间和 level 在第一行，payload 在第二行

### 7.12 流量图优化

- [x] 7.12.1 时间选择器移至图表下方居中
- [x] 7.12.2 1分钟范围X轴显示秒数

### 7.13 页面标题和图标

- [x] 7.13.1 修改页面标题为 "Clash Server"
- [x] 7.13.2 创建六边形 favicon（蓝色渐变）
- [x] 7.13.3 删除旧的 vite.svg
- [x] 7.13.4 侧边栏标题前添加 Logo 图标（36px）
- [x] 7.13.5 侧边栏收缩时只显示 Logo

## 8. 验证

- [x] 8.1 编译测试 `go build ./cmd/server`
- [x] 8.2 验证静态资源访问
- [x] 8.3 验证登录/登出流程
- [x] 8.4 验证 SPA 路由
- [ ] 8.5 验证 PC 端登出入口
- [ ] 8.6 验证仪表盘核心状态显示
- [ ] 8.7 验证订阅卡片激活样式
- [ ] 8.8 验证规则编辑器小屏幕布局
- [ ] 8.9 验证侧边栏流量组件
- [ ] 8.10 验证代理延迟颜色编码
- [ ] 8.11 验证代理组样式
- [ ] 8.12 验证连接信息显示
- [ ] 8.13 验证日志格式
- [ ] 8.14 验证流量图时间选择器
